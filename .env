# Server Configuration
PORT=3001
NODE_ENV=development

# JWT Secret (change this in production!)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Default Admin User
ADMIN_EMAIL=admin@uptake.local
ADMIN_PASSWORD=admin123

# CORS - Frontend URL (comma-separated for multiple origins)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Database connections are managed through the API
# Each connection is stored in the local SQLite database

AI_PROVIDER=gemini
AI_MODEL=gemini-1.5-flash
GEMINI_API_KEY=AIzaSyBsfsy815S9kBOLuBGSC-mVdPLmunjMUfM
AI_DEBUG=false
